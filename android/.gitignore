.gradle/
build/
local.properties
.idea/
# Native libs built from source by jni/build-libssh2.sh — regenerable, not committed.
jni/prebuilt/
jni/spike/ssh_spike
# CMake/ninja externalNativeBuild artifacts
.cxx/

# Release signing — keep these OUT of git and BACK THEM UP.
keystore.properties
*.jks
